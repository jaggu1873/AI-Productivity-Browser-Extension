# Frontend

This folder contains all source code and assets for the browser extension's user interface.

## Structure
- `manifest.json` — Extension manifest (Chrome Manifest V3)
- `popup.html`/`popup.js` — UI and script for browser popup
- `content.js` — (if needed) Content scripts to interact with web pages
- `styles/` — CSS files

## Getting Started
1. Navigate to this folder:

2. Install any required frontend dependencies (if using a build system):

3. Follow instructions in the main README for loading the extension in Developer Mode.

## Notes
- UI/UX mockups and files will go here.
- Coordination for styling, scripts, and new features is done via issues or pull requests.
